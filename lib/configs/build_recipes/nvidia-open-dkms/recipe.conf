NAME=nvidia-open-dkms
DESCRIPTION="NVIDIA open kernel module sources for DKMS"

# Direct install mode — DKMS builds the module in the build root
INSTALL_CMD=/tmp/build/sources/build-nvidia-module.sh

BUILD_DEPS=(
    linux-neptune-616-headers
    pahole
    dkms
    nvidia-open-dkms
)
