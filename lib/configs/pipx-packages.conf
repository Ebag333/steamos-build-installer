# steamos-nvidia-installer pipx packages
# Format: group|package|spec|default|venv|description
# Lines starting with # are comments
# spec is passed directly to `pipx install` or `pipx inject`
# venv groups packages into the same isolated environment.
# First package in a venv runs `pipx install`; subsequent packages run `pipx inject`.

Gaming|linuxgamebench|git+https://github.com/taaderbe/linuxgamebench|TRUE|linuxgamebench|Linux game benchmarking tool
Gaming|PySide6|PySide6|TRUE|linuxgamebench|Qt for Python GUI (injected into linuxgamebench)
